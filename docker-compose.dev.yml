services:
  # Frontend Development Server
  frontend-dev:
    image: node:20-alpine
    container_name: jess-front-dev
    working_dir: /app
    ports:
      - "5273:5273"
    environment:
      - VITE_API_URL=http://localhost:3101/api
      - VITE_PORT=5273
    volumes:
      - .:/app
      - /app/node_modules
    networks:
      - jess-front-dev-network
    command: sh -c "yarn install --frozen-lockfile && yarn dev --host"

networks:
  jess-front-dev-network:
    driver: bridge

volumes:
  frontend_dev_node_modules: