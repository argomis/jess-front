services:
  # Frontend Application
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: jess-front
    ports:
      - "80:80"
    environment:
      - VITE_API_URL=http://localhost:3002/api
    networks:
      - jess-front-network
    restart: unless-stopped

networks:
  jess-front-network:
    driver: bridge